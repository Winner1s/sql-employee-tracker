DROP DATABASE IF EXISTS company;
CREATE DATABASE company;
USE company;

-- first drops the "company" database if it exists, then creates a new "company" database, and finally selects the "company" database for use.